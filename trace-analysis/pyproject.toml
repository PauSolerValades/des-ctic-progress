[project]
name = "trace-analysis"
version = "0.1.0"
description = "CTIC simulation trace parser and metric aggregation"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.0",
    "pyarrow>=14.0",
    "numpy>=1.24",
]
